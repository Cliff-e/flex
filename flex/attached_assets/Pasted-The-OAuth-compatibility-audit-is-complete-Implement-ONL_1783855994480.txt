The OAuth compatibility audit is complete. Implement ONLY the verified fixes required to migrate the backend from the legacy Deriv OAuth flow to the current OAuth 2.0 implementation.

OBJECTIVE

Make the backend fully compatible with the current Deriv OAuth 2.0 authorization server (auth.deriv.com), verify the complete login flow, commit the changes, and push to GitHub.

────────────────────────────────────────
PHASE 1 — Update OAuth implementation
────────────────────────────────────────

Modify ONLY the OAuth implementation in:

artifacts/api-server/src/routes/auth.ts

Make these changes:

1. Replace the authorization endpoint

FROM

https://oauth.deriv.com/oauth2/authorize

TO

https://auth.deriv.com/oauth2/auth

2. Change the authorization request parameter

Replace

app_id

with

client_id

The authorization request should become:

client_id
response_type=code
redirect_uri
code_challenge
code_challenge_method=S256
state

3. Remove the legacy parameter

Remove

l=EN

It belongs only to the legacy oauth.deriv.com implementation.

4. Do NOT change:

✓ token endpoint
https://auth.deriv.com/oauth2/token

✓ PKCE generation

✓ code_verifier

✓ state signing

✓ callback URL generation

✓ exchange endpoint

✓ frontend URLs

✓ environment variables

unless absolutely required.

────────────────────────────────────────
PHASE 2 — Static verification
────────────────────────────────────────

Verify that the generated authorization URL now begins with

https://auth.deriv.com/oauth2/auth

and NOT

https://oauth.deriv.com/oauth2/authorize

Verify the generated query contains

client_id=

and no longer contains

app_id=

Verify the generated URL contains

redirect_uri=https://seahorse-app-lczcg.ondigitalocean.app/api/auth/callback

Verify PKCE still uses

code_challenge_method=S256

────────────────────────────────────────
PHASE 3 — Build verification
────────────────────────────────────────

Run

• TypeScript typecheck

• Build

Start the backend.

Verify:

GET /api/healthz

GET /api/auth/login

Confirm that

/api/auth/login

returns a redirect to

https://auth.deriv.com/oauth2/auth

instead of oauth.deriv.com.

Do not stop verification until you have printed the complete redirect Location header.

────────────────────────────────────────
PHASE 4 — OAuth sanity audit
────────────────────────────────────────

Print the exact authorization URL generated by the backend.

Confirm it contains:

client_id

response_type=code

redirect_uri

code_challenge

code_challenge_method=S256

state

Confirm it does NOT contain:

app_id

l=EN

oauth.deriv.com/oauth2/authorize

────────────────────────────────────────
PHASE 5 — Git
────────────────────────────────────────

After all verification passes:

1. Review git diff.

2. Create a commit with message:

Fix Deriv OAuth2 authorization flow to use auth.deriv.com

3. Push to origin/main.

────────────────────────────────────────
FINAL REPORT
────────────────────────────────────────

Produce:

• Complete diff

• Files modified

• Build output

• Verification output

• Final authorization URL

• Git commit hash

• Git push confirmation

Do not introduce unrelated refactors or architectural changes. Restrict changes strictly to the verified OAuth compatibility fixes.