====================================================
0. Startup Authentication Audit (Highest Priority)
====================================================

DO NOT MODIFY CODE.

Determine why authentication is being attempted during initial application startup.

The intended architecture is:

Application startup:
✓ Public Mode ONLY
✓ Market Engine starts
✓ Charts load
✓ DCircles load
✓ Tick subscriptions
✓ Symbol loading

NO OAuth.
NO authorize().
NO token refresh.
NO account requests.
NO balance requests.
NO portfolio requests.
NO AuthSessionManager initialization.
NO WebSocket authorization.

Authentication should ONLY begin after the user explicitly clicks the Login/Connect to Deriv button.

Trace every startup path beginning at:

main.tsx
↓

App.tsx
↓

Providers
↓

Contexts
↓

Stores
↓

Hooks
↓

useEffect()

↓

AuthSessionManager

↓

WebSocketManager

↓

authorize()

Find exactly who starts authentication.

Identify every startup trigger such as:

- automatic restoreSession()
- initializeAuth()
- authorize()
- refreshToken()
- getCanonicalAuthState()
- autoLogin()
- authenticate()
- authorizeAndSubscribe()
- login state observers
- cookie restoration
- localStorage restoration
- session restoration

For every trigger report:

• File
• Function
• Line
• Call chain
• Why it executes on startup
• Whether it should instead wait for explicit user login

Also verify that:

PUBLIC MODE can fully operate without any authenticated state.

Expected public functionality:

✓ Live market feed
✓ SmartCharts
✓ DCircles
✓ Tick history
✓ AI scanners
✓ Analysis
✓ Symbol loading

Expected authenticated functionality only after Login:

✓ Balance
✓ Account
✓ Portfolio
✓ Buy contracts
✓ Sell contracts
✓ Proposal Open Contract
✓ Transactions
✓ Profit/Loss
✓ Bot execution
✓ Trading history

Return ONLY the investigation.

DO NOT modify any files.
DO NOT generate patches.