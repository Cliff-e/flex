I want the **Manual Trader** and the **AI Scanner** removed from the project completely.

This is **not** a temporary disable or feature flag. I want them permanently deleted from the codebase because they are no longer part of the product, and I suspect they may still be participating in application startup or the authentication lifecycle.

### Remove completely

Delete all code related to:

* Manual Trader
* AI Scanner

This includes, but is not limited to:

* Components
* Pages
* Routes
* Context providers
* Hooks
* Stores
* Services
* Utilities
* Background workers
* Startup initialization
* WebSocket subscriptions
* Event listeners
* Polling
* Timers
* Menu/sidebar/header entries
* Lazy imports
* Dynamic imports
* CSS/assets used only by those features
* Tests for those features

If any shared utility is used only by Manual Trader or AI Scanner after the deletion, remove it as well.

### Clean the project

After deleting them:

1. Remove every import that becomes unused.
2. Remove every dead code path.
3. Remove every route that pointed to them.
4. Remove every feature flag related to them.
5. Remove every initialization that existed only for those features.
6. Ensure there are no orphaned dependencies.

Search the repository for all remaining references to:

* ManualTrader
* Manual Trader
* AIScanner
* AI Scanner
* scanner
* manual trader

There should be **zero runtime references** remaining. Any remaining occurrences should only be historical comments if absolutely necessary.

### Preserve

Do **not** modify the existing authentication architecture except for removing code that belonged exclusively to those deleted features.

Keep intact:

* OAuth / PKCE flow
* CallbackPage
* AuthSessionManager
* AccountModeController
* WebSocketManager
* PublicMarketSocket
* PublicTickManager
* Wallet
* Account balances
* Bot Builder
* Core dashboard

### Verification

After the deletion:

* Build the project.
* Fix any compile errors caused by the removal.
* Remove any unused imports or dependencies introduced by the cleanup.
* Verify the application starts cleanly.

Provide:

1. Every file deleted.
2. Every file modified.
3. Any packages removed because they were only used by these features.
4. Confirmation that neither Manual Trader nor AI Scanner executes any code at startup anymore.
5. A final repository search proving there are no remaining runtime references.

Create a single commit with the message:

`refactor: permanently remove Manual Trader and AI Scanner`

Do not replace them with placeholders or disabled code. I want them completely removed from the application.
