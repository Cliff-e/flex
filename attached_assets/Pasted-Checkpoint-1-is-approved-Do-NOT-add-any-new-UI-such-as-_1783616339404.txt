Checkpoint 1 is approved.

Do NOT add any new UI (such as a Public Mode badge or status indicator).

Before starting Checkpoint 2, follow these constraints.

====================================================
CHECKPOINT 2 OBJECTIVE
====================================================

Separate Public Market initialization from Account initialization.

This is an architectural separation, NOT a rewrite.

Minimize changes.

====================================================
DO NOT
====================================================

Do NOT rename existing public methods unless absolutely necessary.

Do NOT create duplicate implementations.

Do NOT break existing public APIs.

Do NOT change the Login flow yet.

Do NOT modify OAuth yet.

Do NOT modify PKCE yet.

====================================================
INSTEAD
====================================================

Keep the existing initialization flow.

Inside the existing initialization, separate responsibilities into:

1. Public Market initialization

2. Account initialization

The public portion must always execute.

The account portion must execute ONLY after AccountModeController explicitly enables Account Mode.

====================================================
IMPORTANT
====================================================

Do NOT use localStorage, cookies, or existing tokens as the trigger for Account Mode.

The ONLY trigger for Account Mode is:

AccountModeController

If tokens exist but the controller has not enabled Account Mode, they must be ignored during startup.

====================================================
VERIFY
====================================================

After implementation verify that:

✓ Application starts in Public Mode.

✓ No OAuth begins automatically.

✓ No PKCE begins automatically.

✓ No OTP request is made.

✓ No authorize() request is made.

✓ No balance request is made.

✓ No transaction request is made.

✓ Public WebSocket connects.

✓ Public tick stream works.

✓ Active Symbols load.

✓ SmartCharts work.

✓ DCircles work.

Do not continue to Checkpoint 3 until every verification passes.

Return:

1. Files changed.
2. Exact responsibility moved.
3. Verification logs.
4. Any regressions discovered.