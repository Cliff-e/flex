Prompt

The previous implementation is incomplete. Do not build new features. Only fix the existing implementation.

Issue 1 — Virtual Hook never executes virtual trades

Current behaviour:

Virtual Hook is enabled.
The runtime correctly fetches the Virtual account.
The bot enters Virtual Hook mode.
No virtual trades are ever executed.
No virtual contracts appear.
The engine simply waits forever.

Find the exact break in the runtime.

Trace this complete execution path:

Bot.purchase()
    ↓
Purchase.purchase()
    ↓
virtualHookRuntime.isVirtualMode()
    ↓
_executeVirtualTrade()
    ↓
proposal request
    ↓
virtual purchase
    ↓
proposal_open_contract
    ↓
settlement
    ↓
virtualHookRuntime.onVirtualTradeComplete()

Determine exactly where execution stops.

Do not assume the problem.

Add temporary logging around every stage until the missing step is found.

Things to verify:

_executeVirtualTrade() is actually entered.
A proposal is requested.
The proposal callback fires.
A virtual buy request is sent.
The buy response arrives.
proposal_open_contract subscription begins.
Settlement callback executes.
purchaseSuccessful() is dispatched.
virtualHookRuntime.onVirtualTradeComplete() executes.

If any callback never fires, determine why.

Do not replace the architecture.

Do not bypass Virtual Hook.

Only repair the missing runtime flow.

Issue 2 — Purchase Conditions

Modify the default Purchase Conditions to contain only these blocks:

Purchase (All Contract Types)

Hedge

Remove the original Purchase block from the default XML.

The toolbox should also expose only these two purchase blocks for new bots.

Do not change Hedge runtime.

Do not rewrite purchase logic.

Only update the default Purchase Conditions layout.

Constraints
Do not redesign the architecture.
Do not touch Recovery.
Do not touch Contract Switcher.
Do not touch ActiveContract unless it is directly causing the Virtual Hook bug.
Do not duplicate purchase logic.
Reuse the existing Purchase engine.
Before committing

Verify:

Virtual Hook places simulated trades.
Simulated trades settle correctly.
Virtual trade counter increments.
Transition from virtual → real still works.
Transition from real → virtual still works.
Hedge still executes.
Purchase (All Contract Types) still executes.
Existing XML bots continue loading.

Commit only after all checks pass.