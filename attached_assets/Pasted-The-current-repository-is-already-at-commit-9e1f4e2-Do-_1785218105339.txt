The current repository is already at commit **9e1f4e2**. Do **not** reset, rebase, cherry-pick, or overwrite the repository.

I know there are some Blockly/XML Bot Builder changes that existed in **96ffcf9** but are missing from the current commit.

Your task is to **port only those missing Blockly-related changes** from **96ffcf9** onto **9e1f4e2**.

### Requirements

1. Compare:

   * Base (source of missing changes): `96ffcf9`
   * Current branch: `9e1f4e2`

2. Produce a file-by-file diff and identify **only** the files whose Blockly/XML Bot Builder changes are missing.

3. Restrict the work to the Blockly editor only, including files such as:

   * `artifacts/ddbot-app/src/pages/bot-builder/**`
   * `artifacts/ddbot-app/src/external/bot-skeleton/**`
   * Blockly toolbox configuration
   * Blockly categories
   * Blockly block definitions
   * Blockly XML compatibility
   * Blockly menu registration

4. **Do not touch**:

   * Trading engine logic
   * API server
   * Authentication
   * WebSockets
   * Charts
   * Runtime unrelated to Blockly
   * Package files
   * Lockfiles
   * Build configuration
   * Any unrelated files

5. If a file contains both Blockly changes and unrelated modifications, port **only the Blockly portions**. Do not replace the whole file unless the only differences are Blockly-related.

6. Before committing, show me:

   * The list of files that will change.
   * A brief explanation of what is being restored in each file.

7. Verify that:

   * The DDBot app builds successfully.
   * Existing bots continue to load.
   * No unrelated files have changed (`git diff --stat` should show only the intended Blockly files).

8. Commit with:

```text
feat: restore missing Blockly editor changes from 96ffcf9
```

9. Push to `main`.

### Important

Do **not** recreate the entire `96ffcf9` commit. I only want the Blockly/XML Bot Builder improvements that are missing from the current repository. Treat this as a targeted forward-port of the missing editor changes onto `9e1f4e2`, preserving the current repository state.
