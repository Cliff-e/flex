Checkpoint 2 is approved.

Do NOT implement session persistence.

Do NOT add new features.

Do NOT change login behavior beyond the current checkpoint.

The current objective is still architectural separation.

====================================================
CHECKPOINT 3
====================================================

The goal is to eliminate every remaining automatic authentication trigger during application startup.

By the end of this checkpoint:

Application startup must NEVER initiate authentication.

The application may ONLY enter Account Mode through:

AccountModeController.enter({
    fromLoginButton: true
})

No other startup path may transition into Account Mode.

====================================================
REMOVE STARTUP AUTH TRIGGERS
====================================================

Audit and eliminate the remaining startup triggers identified in Checkpoint 2:

• useTMB startup session pre-fetch
• Layout startup AccountModeController.enter(...)
• App URL/localStorage restore
• AuthManager startup refresh cycle

These components must become passive.

They may:

✓ initialize public services
✓ register listeners
✓ expose callbacks

They must NOT:

✗ initiate OAuth
✗ initiate PKCE
✗ restore sessions
✗ fetch OTP
✗ authorize WebSocket
✗ start refresh cycles
✗ enter Account Mode

====================================================
IMPORTANT
====================================================

Do NOT delete the logic.

Move or delay it so it executes ONLY after:

AccountModeController.enter({
    fromLoginButton: true
})

has been called.

====================================================
VERIFICATION
====================================================

Verify a completely cold application start.

Test with:

1. Empty storage.
2. Existing localStorage tokens.
3. Existing cookies.
4. Existing remembered session.

For ALL four scenarios the application must:

✓ Start in Public Mode.
✓ Never attempt authentication automatically.
✓ Never perform OAuth automatically.
✓ Never fetch OTP automatically.
✓ Never authorize automatically.
✓ Continue receiving public market data.

Then verify:

User clicks Login

↓

AccountModeController.enter()

↓

Existing session restored OR OAuth starts

↓

Authenticated services initialize.

Return:

1. Files changed.
2. Startup triggers removed.
3. Verification for all four startup scenarios.
4. Remaining architectural work before regression testing.